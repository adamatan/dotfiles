# Ignore everything by default — remove lines as you adopt each component

# Git
# \.gitconfig

# SSH — permanent ignore: machine-specific, not worth managing via dotfiles
\.ssh

# Claude
\.claude

# bin scripts
bin

# .config subdirectories
# \.config/fish
\.config/immich-sync
# \.config/nix
\.config/opencode
# pj project-jump config is personal (search paths, project list) — never stow
\.config/pj
